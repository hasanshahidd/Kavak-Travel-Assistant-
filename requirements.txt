# --- Core agent (LangGraph orchestration) ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-community>=0.3.0

# --- Vector store + embeddings ---
faiss-cpu>=1.8.0
numpy>=1.26.0

# --- LLM utilities ---
openai>=1.50.0
anthropic>=0.40.0
instructor>=1.6.0
tenacity>=8.5.0
tiktoken>=0.7.0

# --- Data + validation ---
pydantic>=2.8.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# --- CLI + UI ---
rich>=13.7.0
typer>=0.12.0
streamlit>=1.39.0

# --- Dev / quality (kept lean — pinned only what we use) ---
pytest>=8.3.0
ruff>=0.6.0
mypy>=1.11.0
types-pyyaml
